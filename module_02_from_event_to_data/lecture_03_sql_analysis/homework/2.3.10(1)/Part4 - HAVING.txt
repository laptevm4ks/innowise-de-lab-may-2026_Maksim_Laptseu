-- task1

SELECT
    p.product_name,
    SUM(s.total_price) AS total_revenue,
    AVG(s.total_price) AS avg_sale
FROM
    sales s
JOIN
    products p USING (product_id)
GROUP BY
    p.product_name 
HAVING
    SUM(s.total_price) > 400000
ORDER BY
    total_revenue DESC;