Каждый атрибут а данных таблицах является атомарным, так как соблюдает данные условия:
- хранение одного конкретного значения в ячейке.
- разбиение сложных данных на более простые части (ФИО -> Фамилия, Имя, Отчество)
- идентиикаторы являются числовыми, ненулевыми последовательностями.